#include "main.h"
/**
 * mul -function that miltiplies two integers
 *
 * @a: an integer type
 * @b: an integer type
 *
 * Return:Always 0 (Success) and 1 on faol
 */
int mul(int a, int b)
{
	int m = a * b;

	return (m);
}
