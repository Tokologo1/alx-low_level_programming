#include <stdio.h>
/**
 * main - Entry Point
 * @argc: arguments
 * @argv: arry pointing to arguments
 * Return: 0
 */
int main(int argc, char *argc[])
{
	int i = 0;

	while (i < argc)
	{
		printf("%s\n", agrv[i]);
		i++;
	}
	return (0);
